---
sidebar_position: 11
title: "Set Personal Meeting Settings"
hide_title: true
---

<center>

## Set Personal Meeting Settings

</center>

### 简要描述

- Sets personal meeting settings.

### 请求方式

- `POST`

### 请求 URL

- `/rtc-meeting/set_personal_setting`

### Header

| header 名 | 示例值 | 选填 | 类型 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| operationID | 1646445464564 | 必填 | string | 用于全局链路追踪，建议使用时间戳，在每个请求中独立 |
| token | eyJhbxxxx3Xs | 必填 | string | 接口鉴权 token |

### 请求参数示例

```json
{
  "cameraOnEntry": false,
  "meetingID": "string",
  "microphoneOnEntry": false,
  "userID": "string"
}
```

| 字段名 | 选填 | 类型 | 说明 |
| :--- | :--- | :--- | :--- |
| meetingID | 必填 | string |  |
| userID | 必填 | string |  |
| cameraOnEntry | 可选 | *bool | Whether the camera should be enabled by default when joining the meeting. |
| microphoneOnEntry | 可选 | *bool | Whether the microphone should be enabled by default when joining the meeting. |

### 成功返回示例

```json
{
  "errCode": 0,
  "errMsg": "",
  "errDlt": ""
}
```

### 成功返回示例的参数说明

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| errCode | int | 错误码，0表示成功 |
| errMsg | string | 错误简要信息，成功时为空 |
| errDlt | string | 错误详细信息，成功时为空 |

### 数据结构

<a id="ref-openmeeting-meeting-setpersonalmeetingsettingsreq"></a>

#### SetPersonalMeetingSettingsReq

Request to set personal meeting settings.

| 字段名 | 选填 | 类型 | 说明 |
| :--- | :--- | :--- | :--- |
| meetingID | 必填 | string |  |
| userID | 必填 | string |  |
| cameraOnEntry | 可选 | *bool | Whether the camera should be enabled by default when joining the meeting. |
| microphoneOnEntry | 可选 | *bool | Whether the microphone should be enabled by default when joining the meeting. |

<a id="ref-openmeeting-meeting-setpersonalmeetingsettingsresp"></a>

#### SetPersonalMeetingSettingsResp

Response after setting personal meeting settings.

| 字段名 | 选填 | 类型 | 说明 |
| :--- | :--- | :--- | :--- |
| - | - | - | No fields |

